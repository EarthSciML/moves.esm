{
  "esm": "1.0.0",
  "metadata": {
    "name": "F13EnumLeafone",
    "description": "HELPER for F13_enums_collide_across_a_mount.esm, and a positive control: this leaf declares `probe.Symbol` = 1, reads it with an `enum` op, and asserts 1. It PASSES standalone, which is what attributes the mounted failure to the mount. It is excluded from the tripwire stage by name, like join_leaf.esm.",
    "authors": ["EarthSciML Authors and Contributors"],
    "tags": ["finding", "helper", "enums"]
  },
  "enums": { "probe": { "Symbol": 1 } },
  "index_sets": { "leaf_one_rows": { "kind": "interval", "size": 1 } },
  "models": {
    "F13EnumLeafone": {
      "tolerance": { "rel": 1e-12 },
      "variables": {
        "leaf_one_symbolValue": {
          "type": "unknown", "shape": ["leaf_one_rows"], "units": "1",
          "description": "The value this file's own `enums` block gives the symbol."
        }
      },
      "equations": [
        { "lhs": "leaf_one_symbolValue",
          "rhs": { "op": "makearray", "args": [], "regions": [[[1, 1]]],
            "values": [{ "op": "enum", "args": ["probe", "Symbol"] }] } }
      ],
      "tests": [
        { "id": "the_leaf_reads_its_own_enum", "time_span": { "start": 0.0, "end": 0.0 },
          "assertions": [ { "variable": "leaf_one_symbolValue", "time": 0.0, "expected": 1, "coords": { "leaf_one_rows": 1 } } ] }
      ]
    }
  }
}
